# Progressive reveal

Show the parts in the order the learner needs them, not all at once.

A dense final diagram is the end of a sequence, not the start of one. Build it:
introduce the objects, add the relation, then add the constraint that breaks the
naive reading.

Every reveal step is a step the learner could have predicted. If a step arrives
without a question that motivates it, the scene is missing its question.
