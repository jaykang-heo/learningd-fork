# Preserve entity identity

The same semantic object keeps the same id everywhere: `endpoint:e2` is
`endpoint:e2` in the scene graph, in the document SVG, in the video shot and in
the trace ledger.

Implementations may differ; identity may not. This is what lets a learner follow
one object across a representation change instead of re-learning the picture in
every scene.
